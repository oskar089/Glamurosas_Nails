{
  "name": "glamurosas-nails",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "^22.12.0 || >=24.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm --filter @glamurosas/shared run build && pnpm --filter @glamurosas/client run build",
    "start": "pnpm --filter @glamurosas/server run start",
    "preview": "pnpm --filter @glamurosas/client run preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "format": "biome check --write .",
    "format:check": "biome format .",
    "lint": "biome lint ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@playwright/test": "1.63.0"
  }
}
