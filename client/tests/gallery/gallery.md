# Inspiration gallery checks

- Each style filter updates the visible figure count and exposes exactly the intended pressed control.
- Returning to All inspiration restores six images; a classic query link initializes one image.
- Failed external image requests produce a visible, accessible branded fallback without collapsing the photo area.

Photography is blocked deliberately in these behavior checks so the results do not depend on Unsplash availability.
